<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.rit.se.tutorme">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_tutorme"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="edu.rit.se.tutorme.LoginActivity"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize|stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="edu.rit.se.tutorme.TutorProfileActivity"
            android:label="@string/title_activity_tutor_profile"/>

        <activity
            android:name="edu.rit.se.tutorme.SearchTutors"
            android:label="@string/title_activity_search_tutors">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".TutorMainActivity" android:label="@string/title_activity_tutor_main"></activity>
        <activity android:name=".EditTutorPageActivity" android:label="@string/title_activity_edit_tutor_page"></activity>
    </application>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
